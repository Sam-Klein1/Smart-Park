import { View, Text } from "react-native";

export default function Lots() {
  return (
    <View className="flex ">
      <Text>
        Here we should have a list of all the parking lots the user has stored,
        also the "currently displayed" one
      </Text>
    </View>
  );
}
